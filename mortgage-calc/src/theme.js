import { createTheme } from '@mui/material/styles';

export const theme = createTheme({ 
    //importing dark/light theme from @mui/styles
    //You can edit the dark to light depending on how you want the outlook
    palette: {
        mode: 'dark',
    },
})
